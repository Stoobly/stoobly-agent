version: '2.4'
services:
  {service}.mock.configure:
    build:
      dockerfile: ../Dockerfile.sidecar
    extends:
      file: ../docker-compose.base.yml
      service: proxy_configure_base
    profiles:
      - mock
