services:
  build.init_base:
    command: 
    - .stoobly/docker/build/${WORKFLOW_NAME}/bin/.init
    extends:
      file: ../.docker-compose.base.yml
      service: context_base
  build.configure_base:
    command: 
    - .stoobly/docker/build/${WORKFLOW_NAME}/bin/.configure
    extends:
      file: ../.docker-compose.base.yml
      service: context_base
