networks:
  enable-intercept:
    name: enable-intercept
services:
  enable-intercept.init:
    build:
      context: ./
      dockerfile: ../../.Dockerfile.context
    environment: {}
    extends:
      file: ../.docker-compose.base.yml
      service: enable-intercept.init_base
    profiles:
    - test
    volumes:
    - ./dist:/home/stoobly/dist
volumes: {}
